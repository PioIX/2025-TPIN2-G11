export default function RandomRole({ playersAmount }) {
    let players=["pepito2", "etesech", "peroxido", "XxjuancitoGaymerxX", "vusconobia", "permatrago"]
    for (let i = playersAmount - 1; i > 0; i--) {
        const j = Math.floor(Math.random() * (i + 1));
        console.log()
    }
}